// const buttons = document.querySelectorAll(".buttons img");

// buttons.forEach((btn) => {
//   btn.addEventListener("mousemove", function (e) {
//     let t = 1 - (e.offsetY / 31) * 2;
//     let o = -1 + (e.offsetX / 88) * 2;
//     btn.style.transform = `scale(120%) rotate3d(${t}, ${o}, 0, 20deg) translate(${
//       3 * o
//     }px, ${-5 * t}px)`;
//   });
//   btn.addEventListener("mouseleave", function (e) {
//     btn.style.transform = "";
//   });
// });
